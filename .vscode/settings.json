
// Настройки VS Code.
{
    "editor.wordWrapColumn": 100, // Лимит строки, прежде чем будет перенесён символ на новую строку.
    "[ruby]": {
        "files.trimTrailingWhitespace": true // Обрезка висчих строк для руби файлов.
    }
}
